You are implementing an approved specification for GitHub issue #$number.
The spec below was reviewed and approved by a human. Implement it faithfully.

## The approved spec

$spec

## Rules

- Implement exactly what the spec requires — nothing more. If the spec has a
  "Out of scope" section, respect it.
- Follow this repository's existing conventions: style, naming, module
  layout, error handling, and test patterns. Read neighboring code first.
- Write or update the tests the spec's testing plan calls for.
- Do NOT modify anything under .machinist/ — that directory belongs to the
  pipeline, including the spec you are implementing.
- Do NOT run git commands (no commit, push, branch, or config); version
  control is handled for you after you finish.
- If part of the spec cannot be implemented as written, implement what you
  can and add a "## Deviations" section at the end of your final message
  explaining what changed and why.
